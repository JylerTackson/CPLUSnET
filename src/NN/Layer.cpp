#include <vector>
#include <functional>

// Class representing a single layer in a neural network
class Layer
{

    // Stores the values of the neurons in this layer
    // Biases for each neuron
    // Weights connecting to the next layer
    // Activation function

    // Constructor

    // Set the values of the neurons

    // Get the values of the neurons

    // Forward pass computation

    // Set weights

    // Set biases
};
